<?php

namespace App\Services;

class AiAgentService
{
    /**
     * Ponto único de contato com o Microsoft Foundry Agent Service (Azure).
     *
     * A chamada HTTP REST ao provedor de IA (sem SDK) entra aqui futuramente.
     * Mantida vazia nesta etapa — apenas o esqueleto da classe.
     */
    public function processar()
    {
        // TODO: chamada HTTP ao Microsoft Foundry Agent Service entra aqui.
    }
}
